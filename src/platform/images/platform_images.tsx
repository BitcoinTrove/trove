import { ImageCollection, initializeImages } from "../util/image_utils";

export const PlatformImages = {
  Check500x500: null,
  Finished500x500: null,
} as ImageCollection;

initializeImages(PlatformImages);
